# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Fresh build directory
_fresh/
# npm + other dependencies
node_modules/
vendor/

# Generated Vite timestamped configs (not source)
vite.config.ts.timestamp-*.mjs

# Deno KV local database files (environment-specific, do not commit)
kv.db
kv.db-shm
kv.db-wal


.superpowers/

# Local HTTPS dev certificates for on-device testing (machine-specific)
certs/

# macOS Finder metadata
.DS_Store

# Large design handoff archive — kept locally, not committed
docs/Happie-handoff.zip
